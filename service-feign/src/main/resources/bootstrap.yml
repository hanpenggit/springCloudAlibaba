spring:
  application:
    name: nacos-feign
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #注册中心地址
server:
  port: 11000
feign:
  hystrix:
    enabled: true
  httpclient:
    connection-timeout: 100000
  okhttp:
    enabled: true


